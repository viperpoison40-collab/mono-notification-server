# مراجعة وإصلاح خادم MONO على Vercel — المرحلة الثالثة

## ما تم إصلاحه

- استبدال CORS المفتوح بقائمة سماح من `ALLOWED_ORIGINS` مع السماح بطلبات تطبيق Flutter الأصلية التي لا ترسل `Origin`.
- إضافة ترويسات Helmet وإخفاء `X-Powered-By` ومنع تخزين استجابات API الحساسة.
- إضافة `X-Request-ID` لكل طلب لتتبع الأخطاء دون كشف تفاصيل داخلية للعميل.
- إضافة حصص موزعة عبر Firestore لمسارات الإرسال وImageKit الحساسة.
- منع المستخدم المحظور أو المحذوف أو المعطل من استعمال المسارات الحساسة.
- التحقق من عضوية طرفي المحادثة قبل إرسال إشعار رسالة.
- التحقق من سجل المكالمة وهوية المتصل والمستقبل قبل إرسال إشعار المكالمة.
- منع الإشعارات العامة المزورة؛ يجب وجود سجل إشعار مطابق داخل Firestore.
- منع حذف أي ملف ImageKit ما لم تثبت ملكيته داخل منشور/قصة أو يكن المنفذ مديرًا.
- تعطيل مسار تشخيص ImageKit في الإنتاج وعدم إظهار أي جزء من المفتاح.
- إضافة معالج أخطاء موحد لا يعيد تفاصيل الاستثناءات الداخلية.
- تجاهل `patch_backups/` في Git.
- تحديث `firebase-admin` وإضافة `helmet`.

## نتيجة التحقق

- `node --check server.js`: ناجح.
- `npm ci`: ناجح.
- `npm test`: 9 اختبارات ناجحة، 0 فاشلة.
- `npm audit`: 11 تنبيهًا غير مباشر (6 متوسط، 5 مرتفع، 0 حرج).

تنبيهات npm الباقية تأتي من شجرة Google/Firebase غير المباشرة. اقتراح
`npm audit fix --force` الحالي يخفض `firebase-admin` إلى 10.3.0؛ لذلك لم يُستخدم
لأنه تغيير كاسر ويعيد إصدارًا قديمًا. يجب إعادة فحصها عند إصدار تحديث من Google.

## إعداد Vercel

أضف متغير `ALLOWED_ORIGINS` فقط إذا كان لديك Flutter Web أو لوحة ويب:

`https://app.example.com,https://admin.example.com`

لا تضف نجمة `*`. تطبيق Android وiOS الأصلي لا يحتاج هذا المتغير.

## ترتيب النشر

1. شغّل الباتش.
2. نفّذ `npm ci` ثم `npm test`.
3. راجع `git diff` و`git status`.
4. ارفع التعديلات إلى GitHub وانتظر نجاح نشر Vercel.
5. اختبر `/health` ثم اختبر أن `/admin/action` بدون Token يعيد 401.
6. اختبر الرسائل والمكالمات والإعجابات وحذف منشور من التطبيق.

## ملاحظة المرحلة الرابعة

هذه المرحلة أضافت حدًا أوليًا لطلب توقيع ImageKit وتحققًا عند حذف ملف مرتبط.
المرحلة الرابعة ما زالت مطلوبة لبناء حصص رفع حسب النوع والحجم، مسار ملكية لكل رفع،
وتنظيف الرفع غير المكتمل.
