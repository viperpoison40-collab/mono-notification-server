# المرحلة الرابعة — حماية ImageKit

## الحماية المضافة

- توقيع الرفع أصبح `POST` ويتطلب Firebase ID Token صالحًا.
- التحقق من أن الحساب موجود وغير محظور أو محذوف أو معطل.
- فحص نوع الملف وامتداده وحجمه قبل إصدار التوقيع.
- منع المستخدم من اختيار مجلد مستخدم آخر.
- رفع ملفات المحادثة يتطلب عضوية المستخدم في المحادثة.
- إنشاء جلسة حجز لكل رفع باسم ومسار يحددهما الخادم.
- حصة يومية افتراضية لكل مستخدم مقدارها 750 MiB.
- حدود يومية: 60 صورة، 12 فيديو، 60 ملفًا صوتيًا.
- حدود الملف الواحد: صورة 10 MiB، فيديو 250 MiB، صوت 25 MiB.
- بعد الرفع، يجلب الخادم بيانات الملف من ImageKit ويتحقق من المسار والحجم.
- تسجيل `fileId` والمالك والحجم والمسار في `_mediaAssets`.
- إلغاء الجلسة وتحرير الحصة عند فشل الرفع.
- حذف الملف يعتمد أولًا على سجل الملكية، مع دعم الملفات القديمة المرتبطة
  بالمنشورات والقصص.

## متغير اختياري في Vercel

`IMAGEKIT_DAILY_BYTES_PER_USER=786432000`

عدم إضافته آمن؛ القيمة الافتراضية في الخادم هي 750 MiB.

## ملاحظات

- مجموعات `_mediaAssets` و`_mediaUploadSessions` و`_mediaUploadQuotas` لا يمكن
  للعميل قراءتها أو تعديلها بسبب قاعدة الرفض الافتراضية في Firestore.
- التطبيق القديم يستعمل `GET /imagekit-upload-auth`، بينما النسخة الآمنة تستعمل
  `POST`. لذلك يجب إعادة بناء التطبيق بعد نشر الخادم.
- تحسين الفيديو إلى Streaming مستقل وموجود لاحقًا في خطة الإصلاح؛ هذه المرحلة
  تحافظ على طريقة رفع الفيديو الحالية كي لا نخلط تغيير الأمان بتغيير النقل.
